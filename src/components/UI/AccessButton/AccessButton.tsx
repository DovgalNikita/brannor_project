import styles from './AccessButton.module.scss';

const AccessButton = () => {
    return(
        <button className={styles.input}>
            Вход
        </button>
    )
}

export default AccessButton;